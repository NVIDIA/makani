name: Style checks

# Style is PR-scoped: the hooks are fast (~1 min) and the useful signal is
# "does this change conform", not "does every branch tip conform". Unlike
# tests.yml this deliberately omits `push` to avoid duplicate runs.
on:
  pull_request:
  workflow_dispatch:

jobs:
  pre-commit:
    name: Run pre-commit checks
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python 3.12
      uses: actions/setup-python@v6
      with:
        python-version: "3.12"

    # Run pre-commit directly instead of pre-commit/action@v3.0.1, whose
    # bundled sub-actions still target the deprecated Node.js 20 runtime.
    - name: Cache pre-commit environments
      uses: actions/cache@v5
      with:
        path: ~/.cache/pre-commit
        key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

    - name: Run pre-commit
      run: |
        python -m pip install --upgrade pip pre-commit
        pre-commit run --all-files --show-diff-on-failure --color=always
